---
# For configuration options and examples, please see:
# https://gethomepage.dev/configs/info-widgets/

- resources:
    cpu: true
    memory: true
    disk: /

- search:
    provider: duckduckgo
    target: _blank

- unifi_console:
    url: https://192.168.1.1
    username: homepage
    password: fwerq342w3rsy7j8i78K

# Note: Proxmox is not an "info widget" in Homepage.
# To show a Proxmox widget on the Proxmox service card, configure it under
# `services.yaml` (see `proxy/homepage-config/services.yaml`) and provide:
# - HOMEPAGE_VAR_PROXMOX_USERNAME (e.g. api@pam!homepage)
# - HOMEPAGE_VAR_PROXMOX_PASSWORD (API token secret)

